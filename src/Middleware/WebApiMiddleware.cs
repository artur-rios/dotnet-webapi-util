namespace ArturRios.Util.WebApi.Middleware;

/// <summary>Marker base class for custom middlewares that can be registered via <see cref="Configuration.WebApiStartup.AddMiddlewares"/>.</summary>
public abstract class WebApiMiddleware;
